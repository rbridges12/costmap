import numpy as np
import matplotlib.pyplot as plt


def plot_costmap(map_to_cm: np.ndarray, costmap: np.ndarray):
    """
    Plot costmap as an image in matplotlib, assumes cell width is 1.
    # TODO: use pygame
    """
    

def visualize()